<main>
    <h1>Register</h1>
    <p class="form-info">Already registered?
        <a href="/users/login">Login now</a> and have some fun!
    </p>
    <form method="POST">
        <div>
            <input type="email" name="email" placeholder="Email..." value="{{email}}">
        </div>
        <div>
            <input type="text" name="fullName" placeholder="Full name..." value="{{fullName}}">
        </div>
        <div>
            <input type="password" name="password" placeholder="Password">
        </div>
        <div>
            <input type="password" name="repeatPassword" placeholder="Re-password">
        </div>
        <div>
            <p class="message">{{message}}</p>
            <button>Register</button>
        </div>
    </form>
</main>